/* Prevent status icons from rotating when accordion is clicked */
.ui.accordion .title.active .source-check {
  .icons {
    .icon:last-child {
      transform: translateX(-50%) translateY(-50%);
    }
  }
  .icon:not(.dropdown) {
    transform: none;
  }
}
