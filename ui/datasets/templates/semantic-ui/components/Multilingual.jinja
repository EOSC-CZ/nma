{# def data, field #}

<div>
    {% for item in array(data) %}
        {% if item.lang._has_value %}
            <div class="ui label">{{ item.lang }}</div>
        {% endif %}
        {% if item[field]._has_value %}
            {{ item[field]._ui_value | safe }}
        {% endif %}
    {% endfor %}
</div>