{#def data #}
{% for title in array(data) %}
    <span title="{{ title.lang }}">
        <div class="ui green basic label lang-tag">
        {{ title.lang | upper }}
        </div>
        {{ title.title }}
    </span>
{% endfor %}
